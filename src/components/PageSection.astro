---
import SectionHeading from "@components/SectionHeading.astro";
const { id, title, lead } = Astro.props;
---

<section class="animate-on-scroll py-16 min-h-[80dvh]" id={id}>
    <SectionHeading title={title} lead={lead} />
    <slot />
</section>
